// https://github.com/adobe/react-spectrum/blob/main/packages/react-aria-components/src/DatePicker.tsx

describe("DateRangePicker", () => {
  //
  it("provides slots", () => {});

  // slot属性を利用できる
  it("should support the slot prop", () => {});

  // 開いている時に、ボタンの押した状態を利用できる
  it("should apply isPressed state to button when expanded", () => {});

  // data-openを利用できる
  it("should support data-open state", () => {});

  // 描画スクリプトが反映される
  it("should support render props", () => {});

  // form valueを利用できる
  it("should support form value", () => {});

  //
  it("should render data- attributes only on the outer element", () => {});

  // バリデーションエラーを利用できる
  it("supports validation errors", () => {});

  // select = trueの時にcloseを利用できる
  it("should support close on select = true", () => {});

  // select = falseの時にcloseを利用できる
  it("should support close on select = false", () => {});

  // disabledの時ボタンと日付入力が利用できない
  it("should disable button and date input when DatePicker is disabled", () => {});
});
