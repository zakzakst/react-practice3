// https://github.com/adobe/react-spectrum/blob/main/packages/react-aria-components/test/ToggleButton.test.js

describe("ToggleButton", () => {
  // デフォルトのクラスが設定される
  it("should render a toggle button with default class", () => {});

  // カスタムクラスが設定される
  it("should render a toggle button with custom class", () => {});

  // DOMに属性が設定される
  it("should support DOM props", () => {});

  // 描画スクリプトが反映される
  it("should support render props", () => {});

  // slot属性を利用できる
  it("should support slot", () => {});

  // ホバー時の処理が実行される
  it("should support hover", () => {});

  // フォーカス時に線が表示される
  it("should support focus ring", () => {});

  // 押した時に状態が変化する
  it("should support press state", () => {});

  // 無効状態が設定される
  it("should support disabled state", () => {});

  // 選択状態が設定される
  it("should support selected state", () => {});
});
