// https://github.com/adobe/react-spectrum/blob/main/packages/react-aria-components/src/DateField.tsx

describe("DateField", () => {
  //
  it("provides slots", () => {});

  // カスタムクラスが設定される
  it("supports custom class names", () => {});

  // slotを利用できる
  it("should support slot", () => {});

  // ホバー時の処理が実行される
  it("should support hover state", () => {});

  // フォーカス時の処理が実行される
  it("should support focus visible state", () => {});

  // 無効状態が設定される
  it("should support disabled state", () => {});

  // render属性が利用できる
  it("should support render props", () => {});

  // form valueを利用できる
  it("should support form value", () => {});

  //
  it("should render data- attributes only on the outer element", () => {});

  // バリデーションエラーを利用できる
  it("supports validation errors", () => {});

  // バックスペースを押すと前の要素にフォーカスする
  it("should focus previous segment when backspacing on an empty date segment", () => {});
});
