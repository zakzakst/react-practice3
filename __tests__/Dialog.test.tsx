// https://github.com/adobe/react-spectrum/blob/main/packages/react-aria-components/test/Dialog.test.js

describe("Dialog", () => {
  // modalと一緒に挙動する
  it("works with modal", () => {});

  // modalとcustom underlayと一緒に挙動する
  it("works with modal and custom underlay", () => {});

  // isDismissable属性がある時にdismiss buttonがある
  it("has dismiss button when isDismissable", () => {});

  // popoverと一緒に挙動する
  it("works with popover", () => {});

  // triggerにデフォルトのaria labelがある
  it("should get default aria label from trigger", () => {});

  // 描画スクリプトが反映される
  it("should support render props", () => {});

  //
  it("should support Modal being used standalone", () => {});

  //
  it("isOpen and defaultOpen should override state from context", () => {});

  //
  it("supports isEntering and isExiting props", () => {});

  //
  it("portalContainer", () => {});
});
