// https://github.com/adobe/react-spectrum/blob/main/packages/react-aria-components/test/Button.test.js

describe("Button", () => {
  // デフォルトのクラスが設定される
  it("should render a button with default class", () => {});

  // カスタムクラスが設定される
  it("should render a button with custom class", () => {});

  // DOMに属性が設定される
  it("should support DOM props", () => {});

  // フォーム属性に対応している
  it("should support form props", () => {});

  // slot属性を利用できる
  it("should support slot", () => {});

  // ホバー時の処理が実行される
  it("should support hover", () => {});

  // フォーカス時に線が表示される
  it("should support focus ring", () => {});

  // 押した時に状態が変化する
  it("should support press state", () => {});

  // 無効状態が設定される
  it("should support disabled state", () => {});

  // 描画スクリプトが反映される
  it("should support render props", () => {});
});
