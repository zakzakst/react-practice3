// https://github.com/adobe/react-spectrum/blob/main/packages/react-aria-components/src/ComboBox.tsx

describe("ComboBox", () => {
  //
  it("provides slots", () => {});

  // slot属性を利用できる
  it("should support slot", () => {});

  // 開いている時に、ボタンの押した状態を利用できる
  it("should apply isPressed state to button when expanded", () => {});

  //
  it("should support filtering sections", () => {});

  //
  it("should support dynamic collections", () => {});

  // 描画スクリプトが反映される
  it("should support render props", () => {});

  // formValueを利用できる
  it("should support formValue", () => {});

  //
  it("should render data- attributes on outer element", () => {});

  // バリデーションエラーを利用できる
  it("should support validation errors", () => {});

  //
  it("should close on scroll", () => {});
});
