// https://github.com/adobe/react-spectrum/blob/main/packages/react-aria-components/test/Switch.test.js

describe("", () => {
  // デフォルトのクラスが設定される
  it("should render a s with default class", () => {});

  // カスタムクラスが設定される
  it("should render a s with custom class", () => {});

  //
  it("should support data- props on label element", () => {});

  // 描画スクリプトが反映される
  it("should support render props", () => {});

  // slot属性を利用できる
  it("should support slot", () => {});

  // ホバー時の処理が実行される
  it("should support hover", () => {});

  // フォーカス時に線が表示される
  it("should support focus ring", () => {});

  // フォーカスイベントを利用できる
  it("should support focus events", () => {});

  // 押した時に状態が変化する
  it("should support press state", () => {});

  // 無効状態が設定される
  it("should support disabled state", () => {});

  // 選択状態が設定される
  it("should support selected state", () => {});

  // 読み取り専用状態が設定される
  it("should support read only state", () => {});

  //
  it("should render data- attributes only on the outer element", () => {});

  // ref属性を利用できる
  it("should support refs", () => {});

  // inputのref属性を利用できる
  it("should support input ref", () => {});

  //
  it("should support and merge input ref on context", () => {});
});
