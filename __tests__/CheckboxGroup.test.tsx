// https://github.com/adobe/react-spectrum/blob/main/packages/react-aria-components/test/CheckboxGroup.test.js

describe("CheckboxGroup", () => {
  // デフォルトのクラスが設定される
  it("should render a checkbox group with default classes", () => {});

  // カスタムクラスが設定される
  it("should render a checkbox group with custom classes", () => {});

  // DOMに属性が設定される
  it("should support DOM props", () => {});

  // slot属性を利用できる
  it("should support slot", () => {});

  // checkboxの無効状態が設定される
  it("should support disabled state on checkbox", () => {});

  // 無効状態が設定される
  it("should support disabled state on group", () => {});

  // 選択状態が設定される
  it("should support selected state", () => {});

  // 読み取り専用状態が設定される
  it("should support read only state", () => {});

  // バリデーション状態が設定される
  it("should support validation state", () => {});

  // 入力必須状態が設定される
  it("should support required state", () => {});

  // ヘルプテキストが利用できる
  it("supports help text", () => {});

  // 描画スクリプトが反映される
  it("should support render props", () => {});

  // checkboxのaria-describedby属性を利用できる
  it("should support aria-describedby on a checkbox", () => {});

  // バリデーションエラーを利用できる
  it("should support validation errors", () => {});

  // フォーカスイベントを利用できる
  it("should support focus events", () => {});
});
