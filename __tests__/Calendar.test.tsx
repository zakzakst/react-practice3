// https://github.com/adobe/react-spectrum/blob/main/packages/react-aria-components/test/Calendar.test.js

describe("Calendar", () => {
  // デフォルトのクラスが設定される
  it("should render with default classes", () => {});

  // カスタムクラスが設定される
  it("should render with custom classes", () => {});

  // DOMに属性が設定される
  it("should support DOM props", () => {});

  //
  it("should support custom CalendarGridHeader", () => {});

  // slot属性を利用できる
  it("should support slot", () => {});

  //
  it("should support multi-month calendars", () => {});

  // ホバー時の処理が実行される
  it("should support hover", () => {});

  // フォーカス時に線が表示される
  it("should support focus ring", () => {});

  // 押した時に状態が変化する
  it("should support press state", () => {});

  // 選択状態が設定される
  it("should support selected state", () => {});

  // 操作無効状態が設定される
  it("should support unavailable state", () => {});

  // 無効状態が設定される
  it("should support disabled state", () => {});

  // バリデーション失敗状態が設定される
  it("should support invalid state", () => {});

  // 描画スクリプトが反映される
  it("should support render props", () => {});

  // 週表示を利用できる
  it("should support weekdayStyle", () => {});
});
