// https://github.com/adobe/react-spectrum/blob/main/packages/react-aria-components/test/ProgressBar.test.js

describe("ProgressBar", () => {
  // 正しく描画される
  it("renders", () => {});

  // 不確定状態が設定される
  it("supports indeterminate state", () => {});

  // slot属性を利用できる
  it("should support slot", () => {});
});
