// https://github.com/adobe/react-spectrum/blob/main/packages/react-aria-components/test/Tabs.test.js

describe("Tabs", () => {
  // デフォルトのクラスが設定される
  it("should render tabs with default classes", () => {});

  // カスタムクラスが設定される
  it("should render tabs with custom classes", () => {});

  // DOMに属性が設定される
  it("should support DOM props", () => {});

  // aria属性が設定される
  it("should support aria props on the tabs", () => {});

  // 描画スクリプトが反映される
  it("should support render props", () => {});

  // ホバー時の処理が実行される
  it("should support hover", () => {});

  // フォーカス時に線が表示される
  it("should support focus ring", () => {});

  // 押した時に状態が変化する
  it("should support press state", () => {});

  // 全タブの無効状態が設定される
  it("should support disabled state on all tabs", () => {});

  // タブの無効状態が設定される
  it("should support disabled state on tab", () => {});

  // isDisabled属性が設定される
  it("should support isDisabled prop on tab", () => {});

  // 選択状態が設定される
  it("should support selected state", () => {});

  // currentタブが変更されるとTabPanel IDが更新される
  it("should update TabPanel ID when current tab is changed", () => {});

  // 方向が設定される
  it("should support orientation", () => {});

  // ref属性を利用できる
  it("should support refs", () => {});

  //
  it("should support shouldForceMount", () => {});

  //
  it("should support keyboardActivation=manual", () => {});

  // リンクとして利用できる
  it("should support tabs as links", () => {});

  // aria-label属性が付与されてレンダリングされる
  it("should render tab with aria-label", () => {});
});
