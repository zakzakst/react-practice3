import createSpacing from "@/constants/themes/create-spacing";
import { spacingValues } from "@/constants/themes/theme-common";

const spacing = createSpacing(spacingValues);

export default spacing;
